// HadeelBeauty storefront config.
// Update these two values once the backend + TikTok Pixel are ready —
// nothing else in the site needs to change.
window.HADEELBEAUTY_CONFIG = {
  // Base URL of the deployed FastAPI backend (hadeelbeauty-backend), no trailing slash.
  // Example: "https://hadeelbeauty-backend.abcd1234.easypanel.host"
  API_BASE: "https://nakaalaps-hadeelbeauty-backend.ezlcbl.easypanel.host",
  // TikTok Pixel ID for the HadeelBeauty store (leave empty to disable tracking).
  TIKTOK_PIXEL_ID: "",
};
